import { useDispatch } from 'react-redux';

const GamepadListener = (props) => {
  // Create Dispatch Function
  const dispatch = useDispatch();
  console.log('test');
  return null;
};

export default GamepadListener;
